part of 'bag_widgets_imports.dart';

class AddBagItem extends StatelessWidget {
  const AddBagItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
