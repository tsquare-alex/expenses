part of 'commitments_widgets_imports.dart';

class BuildCommitmentsInputs extends StatelessWidget {
  const BuildCommitmentsInputs({Key? key, required this.commitmentsData}) : super(key: key);
  final CommitmentsData commitmentsData;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
      ],
    );
  }
}
