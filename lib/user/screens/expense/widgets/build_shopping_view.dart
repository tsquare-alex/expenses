part of 'expense_widgets_imports.dart';

class BuildShoppingView extends StatelessWidget {
  const BuildShoppingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BuildNoRecord();
  }
}
