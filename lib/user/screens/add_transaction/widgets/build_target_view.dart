part of 'add_transaction_widgets_imports.dart';

class BuildTargetView extends StatelessWidget {
  const BuildTargetView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
