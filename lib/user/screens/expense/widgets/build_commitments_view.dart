part of 'expense_widgets_imports.dart';

class BuildCommitmentsView extends StatelessWidget {
  const BuildCommitmentsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return BuildNoRecord();
  }
}
